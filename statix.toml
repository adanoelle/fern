# statix configuration
# hardware.nix files are generated by nixos-generate-config and must
# never be hand-edited (CLAUDE.md safety rule #1), so style lints
# don't apply to them.
ignore = ["hosts/*/hardware.nix"]
